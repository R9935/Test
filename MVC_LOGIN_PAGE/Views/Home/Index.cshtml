@model List<MVC_LOGIN_PAGE.Models.AddEmp>
@{
    ViewBag.Title = "Home Page";
}


<div id="page-wrapper" >
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1 class="page-header">Employee Details</h1>
            </div>
            <!-- /.col-lg-12 -->
        </div>
        <!-- /.row -->
        <div class="row">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        DataTables Advanced Tables
                    </div>
                    <!-- /.panel-heading -->
                    <div class="panel-body">
                        <div class="table-responsive">
                            <table class="table table-striped table-bordered table-hover" id="dataTables-example">
                                <tr>
                                    <th>E_ID</th>
                                    <th>E_Name</th>
                                    <th>E_Salary</th>
                                    <th>E_Company</th>
                                    <th>E_Dept</th>
                                    <th>E_Email_ID</th>
                                    <th>UserName</th>
                                    <th>password</th>
                                    <th>Delete</th>
                                    <th>Edit</th>

                                </tr>
                                @foreach (var i in Model)
                                {
                                    <tr>
                                        <td>@i.E_ID</td>
                                        <td>@i.E_Name</td>
                                        <td>@i.E_Salary</td>
                                        <td>@i.E_Company</td>
                                        <td>@i.E_Dept</td>
                                        <td>@i.E_Email_Id</td>
                                        <td>@i.UserName</td>
                                        <td>@i.password</td>
                                        <td><a class="btn btn-success" href="@Url.Action("Delete", "Home", new {i.E_ID})">Delete</a></td>
                                        <td><a class="btn btn-danger" href="@Url.Action("Edit", "Home", new {i.E_ID})">Edit</a></td>

                                    </tr>
                                }
                            </table>
                        </div>
                      
                        </div>
                        <!-- /.table-responsive -->
                    </div>
                    <!-- /.panel-body -->
                </div>
                <!-- /.panel -->
            </div>
            <!-- /.col-lg-6 -->
        </div>
        <!-- /.row -->
    </div>
    <!-- /.container-fluid -->


@*<div class="bg-success !important">
        <h1>Details Employee</h1>
        <table class="table table-bordered table-condensed table-hover table-hover table-responsive">
            <tr>
                <th>E_ID</th>
                <th>E_Name</th>
                <th>E_Salary</th>
                <th>E_Company</th>
                <th>E_Dept</th>
                <th>E_Email_ID</th>
                <th>UserName</th>
                <th>password</th>
                <th>Delete</th>
                <th>Edit</th>

            </tr>
            @foreach (var i in Model)
            {
                <tr>
                    <td>@i.E_ID</td>
                    <td>@i.E_Name</td>
                    <td>@i.E_Salary</td>
                    <td>@i.E_Company</td>
                    <td>@i.E_Dept</td>
                    <td>@i.E_Email_Id</td>
                    <td>@i.UserName</td>
                    <td>@i.password</td>
                    <td><a class="btn btn-success" href="@Url.Action("Delete", "Home", new {i.E_ID})">Delete</a></td>
                    <td><a class="btn btn-danger" href="@Url.Action("Edit", "Home", new {i.E_ID})">Edit</a></td>

                </tr>
            }
        </table>
        <div>
            <a align="right" class="btn btn-success" href="@Url.Action("LogOut", "Home")">LogOut</a>
            <a align="left" class="btn btn-warning" href="@Url.Action("Index1" , "Home")">AddEmployee</a>
        </div>

    </div>*@
